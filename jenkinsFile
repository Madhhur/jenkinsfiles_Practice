@Library('My_Shared@main') _

pipeline {
   agent { label 'master' }   // or agent { label 'master' } if your node is actually called master
    
    stages {
        stage("first") {
            steps {
                script {
                    echo "========first stage========"
                    gitCheckout([
                        url: 'https://github.com/Madhhur/repo1.git',
                        branch: 'main',
                        credentialId: 'Madhhur'
                    ])
             
                }
               
            }
        }
    }

}
