@Library('My_Shared@main') _

pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                script {
                    // This calls your library method outside sandbox restrictions
                   def config = [
                        url: 'https://github.com/Madhhur/repo1.git',
                        branch: 'main',
                        credentialId: 'Madhhur'
                        ]
                    gitCheckout(config)
                        
                    
                }
            }
        }
    }
}
